language: go

go:
  - stable
  - master

before_script:
  - go get github.com/golang/lint/golint

script:
  - test -z $(gofmt -l .)
  - test -z $(golint ./...)
  - go vet ./...
  - go test -v ./...

matrix:
  allow_failures:
    - go: master
