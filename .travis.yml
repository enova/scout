language: go

go:
  - 1.6
  - 1.7
  - tip

before_script:
  - go get github.com/golang/lint

script:
  - test -z $(gomft -l .)
  - test -z $(golint ./...)
  - go vet ./...
  - go test -v ./...

matrix:
  allow_failures:
    - go: tip
